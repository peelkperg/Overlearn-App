# Expo / React Native
node_modules/
.expo/
.expo-shared/
dist/
web-build/
expo-env.d.ts

# Generated from app.json's baseUrl (AD-3) -- never hand-committed
public/manifest.json

# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# Debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# Local env files
.env*.local

# TypeScript
*.tsbuildinfo

# EAS
credentials.json

# Editor
.vscode/
.idea/

# Generated native folders (Expo managed workflow / dev-client prebuild)
/ios
/android

# BMad personal overrides (team overrides in _bmad/custom/*.toml are committed)
_bmad/custom/*.user.toml
_bmad/config.user.toml

# BMad installer-generated manifests (regenerated per-install, timestamped)
_bmad/_config/
*.bak

# Claude Code: skills are project-local BMAD content (committed); these two
# files are per-machine/session state, not project config.
.claude/settings.local.json
.claude/scheduled_tasks.lock
