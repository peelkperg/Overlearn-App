# Story 5.2: Apply a Changed Target to an In-Progress Session

Status: done

<!-- Note: Validate with validate-create-story before dev-story if desired. -->

## Story

As a user,
I want a target change I make mid-session to take effect immediately,
so that the app never operates on two different rules at once.

## Acceptance Criteria

1. **Given** a session is in progress **When** the user changes the overlearning-% in Settings and returns to that session **Then** the displayed target streak reflects the new percentage immediately, with no restart or re-navigation required (FR37)
2. **Given** a session is in progress with `current_streak` already meeting or exceeding what the recalculated target would be **When** the user lowers the overlearning-% in Settings **Then** the session completes immediately as a result of the setting change alone — `session_complete` flips true, `completed_target` captures the achieved streak (not the newly-lowered target, if the streak exceeds it), the FR22 lockout applies, and the same completion feedback (haptic, screen-reader announcement) fires as a Correct-triggered completion (FR37, FR22, FR12, UX-DR3) (added 2026-09-12, code review round 2 — this file's own AC section had fallen out of sync with epics.md, which already carried this AC from round 1)

[Source: _bmad-output/planning-artifacts/epics.md#Story-5.2-Apply-a-Changed-Target-to-an-In-Progress-Session]

**Note from epics.md:** per architecture.md, this requires no new mechanism for the target *number* — it verifies the behavior Story 5.1's plumbing already produces, since `target_streak` is derived on every read. Same shape as v1.0's Story 2.9, which verified an already-built guarantee rather than building a new one. Tasks 2 and 3 below exist because Story 5.1's own code review (2026-09-10) found two adjacent gaps this "just verify" framing doesn't cover — see Dev Notes.

## Tasks / Subtasks

- [x] Task 1: End-to-end verification test for AC #1 (AC #1)
  - [x] In `src/app-tests/session.test.tsx`, add a test that: renders `ActiveSessionScreen` for a segment, logs some correct/incorrect taps (session stays incomplete), calls `setOverlearningPercent()` (from `@/lib/settings`) directly — simulating "user backed out to Settings and changed it" without navigating away from this screen (Home stays mounted underneath per the app's existing navigation model; this screen itself never unmounts on that round trip) — then asserts `streak-readout`'s target (`props.children[2]`) reflects the new percentage on the *next render*, with no call to `start()`/`restart()`. This is the literal AC #1 scenario; nothing in the codebase currently exercises it (`useActiveSession.test.ts`'s existing level-threading tests all set the level *before* `start()`, not mid-session).
  - [x] Assert the same at the hook level too, in `src/hooks/useActiveSession.test.ts`: `start()`, log some taps, `setOverlearningPercent()` to a different value, then re-read `result.current.targetStreak` on the next render (no explicit re-render call needed — `useSettings()`'s `useSyncExternalStore` subscription already triggers one) and confirm it reflects the new level.

- [x] Task 2: Anchor the Completion screen's displayed target to what actually gets recorded (AC #1's underlying guarantee — see Dev Notes)
  - [x] In `src/app/session/[id].tsx`, change the `<CompletionScreen ... finalTarget={targetStreak} .../>` prop (line ~151) to `finalTarget={session.completedTarget ?? targetStreak}`. `session.completedTarget` was added in Story 5.1's code review fix (`src/lib/types.ts`) specifically so `complete()`'s written `finalTarget` can't drift from a live setting change between completion and Done — but the *displayed* Completion screen still reads the live `targetStreak`, so a settings change made while sitting on the Completion screen (before tapping Done) would show the user one number while a different one gets permanently recorded. The `?? targetStreak` fallback is defense-in-depth only, matching the codebase's established style for a state that should be unreachable (`session.sessionComplete` is only ever set `true` in the same write that sets `completedTarget`, per `session-transitions.ts`'s `logCorrect`).
  - [x] Add a test in `src/app-tests/session.test.tsx`: reach completion, call `setOverlearningPercent()` to a different value *before* asserting on `completion-stats`, and confirm the displayed "Target reached: N" still shows the value achieved, not the new live one.

- [x] Task 3: Verify (and, if it reproduces, fix) the deferred `sessionComplete` staleness edge case (see Dev Notes — Deferred Finding)
  - [x] Write a failing-first test reproducing the exact scenario logged in `_bmad-output/implementation-artifacts/deferred-work.md` ("Deferred from: code review of 5-1..."): start a session, log enough mistakes to raise the target well above the floor, lower the overlearning-% via `setOverlearningPercent()` enough that the *current* streak already meets or exceeds the *new* (lower) live target, and assert what `session.sessionComplete` / the rendered screen actually do. **Confirmed reproduced** — `session.sessionComplete` stayed `false` under the current (pre-fix) code (`src/hooks/useActiveSession.test.ts`, `[Story 5.2, Task 3]`).
  - [x] **Reproduced** (current streak ≥ live target but `sessionComplete` stayed `false`): implemented the reconciliation approach from Dev Notes — a `useEffect` inside `useActiveSession()`, keyed on `overlearningLevel` alone, reads the session fresh via `sessionStore.readSession()` (not the render closure — matches every other mutator's stale-closure guard) and, if incomplete with `currentStreak >= liveTarget`, writes `{ ...current, sessionComplete: true, completedTarget: liveTarget }`. `logCorrect`/`logIncorrect`'s no-op guards were left unchanged, as specified.
  - [x] Read `useActiveSession.ts`'s header comment (rapid-tap compounding, two-mounted-instances) before implementing — confirmed safe: the effect reads fresh at fire time (immune to a stale render-closure race with a concurrent tap), and the write is idempotent across multiple mounted instances (same deterministic inputs → same computed output), so no HALT was needed.
  - [x] Recorded in Completion Notes below; `deferred-work.md`'s entry updated with a struck-through summary and a "Resolved by Story 5.2 (Task 3)" note — kept rather than deleted, so the resolution has a visible record; this is the first entry in the file to be resolved, so it establishes rather than follows a convention.

### Review Findings

Code review 2026-09-11 (3 adversarial layers: Blind Hunter, Edge Case Hunter, Acceptance Auditor; all 3 completed). 3 decision-needed, 12 patch, 2 deferred, 1 dismissed.

**Decision-needed — all 3 resolved by Gerardo, 2026-09-11:**

- [x] [Review][Decision] **RESOLVED — keep the effect; specify the consequences (option 1).** The settings-driven completion trigger stays as designed. Its consequences are accepted as intended behavior and must now be written into the specs rather than removed from the code: (1) a single stepper tap can end an in-progress session irreversibly; (2) changing the percent while on Settings ejects the user to the Completion screen mid-adjustment; (3) FR22's lockout disables Restart without a tap; (4) only Done or Repeat remain as exits; (5) **an interrupted session can auto-complete at app open, bypassing the FR24 Resume/Discard prompt** — the effect also runs on mount, and `showResumeDialog` requires `!session.sessionComplete`. Consequence (5) was not anticipated by the original deferred note and defeats a guarantee Story 2.6 was built to provide; it is accepted knowingly. See the spec-synchronization patch items below for the full list of documents this obligates.
- [x] [Review][Decision] **RESOLVED — record the achieved streak.** `completedTarget` for a settings-driven completion becomes `Math.max(liveTarget, current.currentStreak)`, so "Target reached: N" never claims less work than the user actually performed. Rationale: `HistoryEntry` has no separate field for the achieved streak, so recording the lower live target would permanently discard the fact that the user reached 20 consecutive correct. Tap-driven completion is unaffected (it always lands on `currentStreak === targetStreak` exactly, so the `max` is a no-op there).
- [x] [Review][Decision] **RESOLVED — FR37 is authoritative; amend the PRD.** prd.md (~line 287)'s "never falls, except via Restart" is corrected to hold *within a fixed overlearning level*: the target rises or holds as mistakes accrue, and changes otherwise only via Restart or an explicit overlearning-% change (FR37, v1.1). The sentence predates v1.1 and was never reconciled when FR37 was added in Story 5.1.

**Superseded decision text (kept for audit):**

- [x] [Review][Decision] ~~**Should a settings change flip `sessionComplete` at all?**~~ — resolved above as option 1. — Task 3's reconciliation `useEffect` (`src/hooks/useActiveSession.ts:41-49`) introduces a fourth completion trigger that no spec defines. PRD's Mechanic Specification (~line 289) makes completion a consequence of the **Correct** transition only; FR37 (prd.md:354) and AC #1 scope immediacy to "the target-streak *calculation*" / "the *displayed* target streak", never to `session_complete`. Three concrete user-visible consequences, all confirmed against the real code: **(a)** Home mounts `useActiveSession()` (`src/app/index.tsx:33`) and holds a redirect effect (`src/app/index.tsx:78-85`) that fires `router.replace('/session/<id>')` the moment `sessionComplete` turns true — so stepping the percent down while on Settings *replaces the Settings screen* with the Completion screen mid-adjustment, with no back route and no way to undo the value just set; **(b)** the flip is irreversible — the effect early-returns on `current.sessionComplete`, so stepping back up does not undo it, and FR22's lockout (`logCorrect`/`logIncorrect`/`restart` all no-op once complete) means an accidental one-tap stepper adjustment ends the in-progress session, exitable only via Done (permanently recording it) or Repeat (discarding the run); **(c)** it silently disables Restart (FR20/FR21) with no AC, FR, or UX statement authorizing it. Options: (1) keep the effect and accept/spec these consequences, (2) scope the effect to the Active Session screen only, (3) drop the stored-flag reconciliation and derive completion on read instead, (4) revert Task 3 and re-defer the original finding to Story 5.3 (which already owns the mid-session-change warning, FR39).
- [x] [Review][Decision] ~~**What target should a settings-driven completion record?**~~ — resolved above: record the achieved streak.
- [x] [Review][Decision] ~~**PRD spec conflict: target monotonicity vs. FR37.**~~ — resolved above: FR37 authoritative, amend the PRD sentence.

**Patch — arising from the resolved decisions:**

- [x] [Review][Patch] Record the achieved streak on settings-driven completion: `completedTarget: Math.max(liveTarget, current.currentStreak)` [src/hooks/useActiveSession.ts:47]
- [x] [Review][Patch] Amend prd.md's monotonicity sentence (~line 287) to hold within a fixed overlearning level; target changes otherwise only via Restart or an explicit overlearning-% change (FR37, v1.1) [prd.md:~287]
- [x] [Review][Patch] PRD Mechanic Specification (~line 289): add the settings change as a fourth completion trigger alongside Correct/Incorrect/Restart [prd.md:~289]
- [x] [Review][Patch] PRD FR22: state that the completion lockout is reachable without a Correct/Incorrect tap, via an overlearning-% change [prd.md]
- [x] [Review][Patch] PRD FR24 / Story 2.6: state that an interrupted session may auto-complete at app open, bypassing the Resume/Discard prompt [prd.md]
- [x] [Review][Patch] Story 5.2 AC #1 in epics.md: AC covers only the *displayed target*; extend it to cover settings-driven completion, or add an AC that does [epics.md:762-774]
- [x] [Review][Patch] Story 5.3's notice copy ("Changing this target updates it immediately") understates "may end this session"; its AC also bars the per-tap confirmation that would normally carry that warning — reconcile before 5.3 is implemented [epics.md:776-795]

**Patch — defects, independent of the decisions:**

- [x] [Review][Patch] `SessionState.completedTarget` added with no schema migration — every in-progress session persisted by the shipped build is quarantined and silently lost on upgrade [src/lib/types.ts:138]
- [x] [Review][Patch] Reconciliation effect's `writeSession` is unguarded — a storage failure throws out of the effect body and crashes instead of degrading [src/hooks/useActiveSession.ts:47]
- [x] [Review][Patch] Settings-driven completion fires no `feedback.completion(...)` — no success haptic and no screen-reader announcement, unlike every tap-driven completion (FR12/UX-DR3) [src/hooks/useActiveSession.ts:41-49]
- [x] [Review][Patch] Reconciliation hand-builds `SessionState` in the hook layer, duplicating `logCorrect`'s completion rule outside `lib/session-transitions.ts` (CLAUDE.md §5.5) [src/hooks/useActiveSession.ts:47]
- [x] [Review][Patch] ~~Effect fires on mount and carries no `segmentId` identity check~~ — **no code change; reclassified on inspection.** `useActiveSession()` takes no segmentId and has never had a per-session identity concept: `session.active` is a single MMKV key app-wide (architecture.md), so there is only ever one active session for the whole app to reconcile against — there is no second session it could mistakenly write to. The mount-firing behavior itself (not the "identity" framing) is real and is exactly consequence (5) accepted under the first decision above; it is not an independent defect once that decision is made. [src/hooks/useActiveSession.ts:41-49]
- [x] [Review][Patch] Two comments added by this change assert `logCorrect` is the only writer of `sessionComplete`, which the same change falsifies [src/app/session/[id].tsx:150-157, src/hooks/useActiveSession.ts:150-157]
- [x] [Review][Patch] Negative reconciliation test is vacuous — `setOverlearningPercent(50)` when 50 is already the value never re-fires the effect; passes with the guard deleted [src/hooks/useActiveSession.test.ts:125-140]
- [x] [Review][Patch] No coverage of `currentStreak > liveTarget` or `liveTarget - 1`; the `>=` operator could be `===` and the suite stays green [src/hooks/useActiveSession.test.ts:91-123]
- [x] [Review][Patch] Task 3's own subtask specifies a screen-level assertion ("the rendered screen") that was never written — no test asserts `ActiveSessionScreen` swaps to `CompletionScreen` after a settings-only change [src/app-tests/session.test.tsx]
- [x] [Review][Patch] `architecture.md`'s FR37 paragraph still reads "No session-state field changes, no explicit propagation code" — falsified by this change, which adds both; `types.ts` file-tree line also left stale while its sibling was corrected (CLAUDE.md §13.4) [architecture.md:573, ~670]
- [x] [Review][Patch] Dev Agent Record inaccuracies: "Git intelligence" claims `27a168a` contains Story 5.1's post-review fixes (it does not — they are uncommitted in this changeset); File List names 5 files against 18 actually changed; "lint and typecheck clean" is false (`npx eslint .` exits with 1 error + 2 warnings, all pre-existing and untouched here) [this file]
- [x] [Review][Patch] Story 5.1's unshipped post-review work is commingled with Story 5.2 in one changeset, and 5.1 is flipped to `Status: done` for code that exists only in the working tree (CLAUDE.md §13.2) — split into two commits before landing [changeset-wide] — **Resolved 2026-09-12, per Gerardo's authorization: Story 5.1's post-review fixes were already committed separately in `c37b2a8` (confirmed via `git log`), so no split was actually needed for that pairing. The working tree instead also carried Story 5.4's own uncommitted work, discovered at commit time — that was split into its own commit rather than folded in here. Two files (`session-transitions.test.ts`, `epics.md`) contain unsplittable hunks from both 5.2 and 5.4 (no `-i` staging available); per Gerardo's decision, both are committed whole with this story, and this story's commit message notes the small 5.4 UX-DR1 hunk that rides along in `epics.md`.**

**Deferred (pre-existing, logged to deferred-work.md):**

- [x] [Review][Defer] Stepper `disabled`/style still read the stale render-time `settings.overlearningPercent` while `onPress` reads fresh — masked today by `setOverlearningPercent`'s clamp [src/app/settings.tsx:27,47] — deferred, pre-existing
- [x] [Review][Defer] Disabled-stepper tests assert only the storage clamp, not that the control is inert — both pass with `disabled` removed [src/app-tests/settings.test.tsx:~252] — deferred, pre-existing

**Dismissed:** `completedTarget` stickiness across Repeat — `handleRepeat` calls `start()` → `startSession()`, which writes a fresh `completedTarget: null`; unreachable.

### Review Findings — Round 2 (2026-09-12)

Second adversarial review, run under Opus 5 against commit `93b0331` (which already
contains round 1's applied patches). Three layers: Blind Hunter (diff only, no project
access), Edge Case Hunter (diff + project), Acceptance Auditor (diff + specs). 32 raw
findings → 25 unique after merging 7 duplicate pairs → 1 dismissed. All contested claims
were independently verified against the real code before triage.

Round 2 concentrates on round 1's own patch work — `reconcileCompletion`, the v1→v2
migration, the `feedbackRef`, and the spec synchronization the three resolved decisions
obligated. Two independent layers converged on the migration defect (first item below).

#### Decisions required

**Both resolved 2026-09-12 by Gerardo.**

**Decision 1 → option 1: one rule.** `logCorrect` delegates its completion branch to `reconcileCompletion`, making it the single implementation of "streak ≥ target ⇒ complete, capture `completedTarget`". `logCorrect` consequently also records `max(targetStreak, currentStreak)` in the drift case — consistent with the round-1 decision's own rationale that a completed session must never claim less work than the user actually performed. `reconcileCompletion`'s early return on an already-complete session subsumes the `session.completedTarget ?? ...` re-invocation guard it replaces, so that guarantee survives the refactor by a different mechanism. The false "shared with logCorrect" claims in the commit record and the in-code comment become true rather than being deleted.

**Decision 2 → option 2: a miss cannot destroy a completed run.** The `logIncorrect` path retries the missed reconciliation before zeroing the streak: if the session is already complete-in-fact (`currentStreak >= live target`) and only failed to be written as such, the Incorrect tap completes it instead of discarding the run, and FR22's lockout applies from that point. This makes the effect's "the next tap will retry the same reconciliation" comment true for both tap paths rather than only for Correct. Silent degradation on the write itself is still accepted — no new error surface is added — so the comment must stop claiming a recovery route that does not exist for the no-further-taps case.

- [x] [Review][Decision] ~~**The completion rule was moved, not de-duplicated — and the commit says otherwise.**~~ — resolved above as option 1. The commit message claims "Extracted the completion rule into `session-transitions.ts`'s `reconcileCompletion`, **shared with logCorrect**", and the in-code comment repeats that it "is the single source of the completion rule shared with logCorrect". Verified false: `logCorrect` (`session-transitions.ts:35-56`) is untouched by the diff and still computes its own `currentStreak >= targetStreak` and its own `completedTarget` capture. Two independent copies of the completion rule now live in the same module — and they **already disagree**: `logCorrect` records `targetStreak`, `reconcileCompletion` records `max(targetStreak, currentStreak)`. The round-1 patch item ("Reconciliation hand-builds `SessionState` in the hook layer, duplicating `logCorrect`'s completion rule outside `lib/session-transitions.ts`") is checked off on the strength of a claim the code contradicts: the layer violation was fixed, the duplication was not. The disagreement is reachable — after a failed reconciliation write (see the write-failure decision below), a Correct tap runs `logCorrect` and records the *lower* recalculated target for a run the user overshot, which is precisely what the "record the achieved streak" decision ruled against. Options: (1) `logCorrect` delegates its completion branch to `reconcileCompletion` — one rule, and `logCorrect` then also records `max(...)` in the drift case (consistent with the resolved decision's own rationale); (2) keep two rules and correct the false claims in the commit record and the comment only; (3) keep two rules but add a test pinning that they agree on every shared input. [src/lib/session-transitions.ts:35-56, 89-98]
- [x] [Review][Decision] ~~**A failed reconciliation write leaves a self-contradictory session, and the comment's stated recovery is false.**~~ — resolved above as option 2. `catch { return; }` (`useActiveSession.ts:96-106`) swallows a `writeSession` throw. The session stays `sessionComplete: false` with `currentStreak` above its own live target, so the Active Session screen renders `20 / 6` with Correct/Incorrect still live and **no error text** — unlike every other write on that screen, which routes through `runAction` and sets `error`. The effect cannot retry itself: its only dependency is `overlearningLevel`, which does not change again. The comment claims "the next tap … or settings change will retry the same reconciliation" — true for a Correct tap, but **false for Incorrect**: `logIncorrect` (`session-transitions.ts:63-69`) zeroes `currentStreak` before anything re-evaluates completion, so one Incorrect tap silently discards the achieved run and the completion is lost with no notice. `feedback.completion(...)` also never fires on this path (it sits after the try/catch). CLAUDE.md §4.1.D makes undefined failure handling a halt condition. Options: (1) surface the failure through the screens' existing `error` mechanism; (2) reconcile on the `logIncorrect` path too, so a miss cannot silently destroy a completed run; (3) accept silent degradation and correct only the false comment. [src/hooks/useActiveSession.ts:89-111]

#### Patches

**Arising from the resolved decisions:**

- [x] [Review][Patch] Make `reconcileCompletion` the single completion rule: `logCorrect` builds the advanced state (streak + 1, totalCorrect + 1) and returns `reconcileCompletion(advanced, overlearningLevel)` instead of re-implementing the `>=` check and the `completedTarget` capture. Correct the in-code comment and note the behaviour change for the drift case. [src/lib/session-transitions.ts:35-56, 89-98]
- [x] [Review][Patch] Retry the missed reconciliation on the `logIncorrect` path before zeroing the streak, so an Incorrect tap cannot silently discard an already-achieved completion; fire the same completion feedback as the other paths when it does complete. [src/hooks/useActiveSession.ts]
- [x] [Review][Patch] Correct the reconciliation effect's `catch` comment: "the next tap … or settings change will retry" is true for both tap paths only after the above, and remains false when no further interaction occurs — state the accepted silent-degradation behaviour rather than implying guaranteed recovery. [src/hooks/useActiveSession.ts:96-106]
- [x] [Review][Patch] Add tests for both resolved decisions: that `logCorrect` and `reconcileCompletion` agree on every shared input (one rule, verified), and that an Incorrect tap on an un-reconciled satisfied session completes it rather than zeroing the streak. [src/lib/session-transitions.test.ts, src/hooks/useActiveSession.test.ts]

**Independent of the decisions:**

- [x] [Review][Patch] **The v1→v2 migration re-introduces the exact bug Task 2 was written to fix, for already-complete sessions** (flagged independently by two layers; Edge Case Hunter confirmed reachability). `migrations[1]`'s condition keys on the *presence* of `completedTarget`, not its value, so a session that was already `sessionComplete: true` on the shipped build is migrated to `completedTarget: null` exactly like an in-progress one. `reconcileCompletion` early-returns on `session.sessionComplete`, so nothing ever backfills it. Both `?? targetStreak` fallbacks then fire: the Completion screen displays, and `complete()` permanently records, a live-recomputed target the user never practiced under — the display/history divergence Task 2 exists to prevent. Both fallback sites carry comments asserting this state is unreachable; this migration is what makes it reachable. Fix is provable, not a judgement call: under any build that lacks `completedTarget`, `logCorrect` was the only completion path and completes at `currentStreak === targetStreak` exactly, so set `completedTarget: data.currentStreak` when `sessionComplete` is true. [src/lib/storage.ts:95-100]
- [x] [Review][Patch] **Accepted consequence (5) contradicts four specs that were never amended.** The decision accepted that an interrupted session can auto-complete at app open, bypassing FR24's Resume/Discard prompt, and the patch item "PRD FR24 / Story 2.6" is checked — but only `prd.md:396` was amended. Still standing unqualified: NFR4 (`prd.md:424`, `epics.md:158`) "the app must never silently resume or silently discard"; UX-DR5 (`epics.md:191`) "never silently skipped (NFR4)"; Story 2.10's AC (`epics.md:624`) "never silently resumed or silently discarded"; `ux-design-specification.md:353` "step `E→G` is mandatory whenever an interrupted session exists". NFR4 is the stronger statement the amended FR24 now contradicts. Code confirms the bypass: `app/index.tsx:73-77` gates `showResumeDialog` on `!session.sessionComplete`, and the effect runs on mount. [prd.md:424, epics.md:158, 191, 624, ux-design-specification.md:353]
- [x] [Review][Patch] **`useActiveSession` has a third mount point, so navigating to Settings alone can complete a session.** `app/settings.tsx:28` mounts the hook (alongside `app/index.tsx:34` and `app/session/[id].tsx:33`), so the effect's first run fires on that navigation with no value change — success haptic and screen-reader announcement included — and Home's redirect effect then replaces Settings before the user sees it. Both the hook's header comment and the new effect's comment reason explicitly about "two mounted instances"; the accepted-consequence list names a settings *change* and *app open*, not navigation. The write itself is idempotent across instances (verified). [src/hooks/useActiveSession.ts:89-111, src/app/settings.tsx:28]
- [x] [Review][Patch] **`epics.md:261` still says FR37 "needs no new mechanism"** — the twin of the `architecture.md` sentence this commit corrected, left stale. [epics.md:261]
- [x] [Review][Patch] **`architecture.md:202` and `epics.md:178` still assert `session_complete` is written only in the same MMKV write as the triggering `current_streak` update, "not a separate mechanism"** — falsified by `reconcileCompletion`, which writes it with `currentStreak` untouched from an effect keyed on `overlearningLevel`. [architecture.md:202, epics.md:178]
- [x] [Review][Patch] **`completed_target` is a persisted session field absent from both authoritative schema lists** — the PRD's Session State table (declared authoritative at `prd.md:306`) lists six fields without it, while the transitions immediately below now reference it; `architecture.md:196`'s persisted-field enumeration likewise omits it, despite that document's own convention of an explicit "Schema addition" body entry per field (`total_correct_this_session`, `segment_id`). [prd.md:310-317, architecture.md:196]
- [x] [Review][Patch] **Two of the five accepted consequences were never written into any spec**, though the decision obligated it and the covering patch items are checked: consequence (2), being ejected from Settings to the Completion screen mid-adjustment — no navigation statement exists in `prd.md`, `epics.md`, or the UX spec; consequence (3), FR22's lockout disabling Restart without a tap — FR22's amendment covers only "repetition input", and FR20/FR21 carry no annotation even though `restart()` also no-ops once complete. [prd.md:411, epics.md:884, prd.md:388-389]
- [x] [Review][Patch] **The UX spec still tells the user a target change is reversible.** `ux-design-specification.md:599` "Changing a target is reversible by stepping back" and `:586` "the user can step back to the previous value just as easily" — directly contradicted by accepted consequence (1), which records that the flip is irreversible because the effect early-returns on `sessionComplete`. [ux-design-specification.md:586, 599]
- [x] [Review][Patch] **"Target reached: N" can display a number that was never a target, with no spec reconciliation.** `completedTarget` now holds `max(targetStreak, currentStreak)`, rendered verbatim by `CompletionScreen` and asserted as `Target reached: 20` when the recalculated target was 6. FR13, FR28 and UX-DR4 all still read "the final target achieved". The screen-reader announcement compounds it by building from `next.currentStreak` rather than `next.completedTarget` — two sources for one number. [src/hooks/useActiveSession.ts:110, prd.md:378, 403, epics.md:190]
- [x] [Review][Patch] **No test at any level covers the mount-time / app-open auto-completion** — the one consequence the decision itself flagged as unanticipated. Every Task 3 test changes the level *after* mounting; none mounts over a pre-existing on-disk session that the effect completes, and none asserts the Resume/Discard dialog is suppressed in that case. A later change adding a mount guard would delete the accepted behavior with the suite still green. [src/app-tests/index.test.tsx, src/hooks/useActiveSession.test.ts]
- [x] [Review][Patch] **The story file's own Acceptance Criteria section was never extended with AC #2.** `epics.md:882-884` gained the settings-driven-completion AC; this file still lists exactly one AC and the note below it still frames Tasks 2–3 as gaps outside the AC. This file is what dev and review layers read as authoritative for the story. [this file:13-19]
- [x] [Review][Patch] **A test tripwire was removed, not fixed.** `storage.test.ts:100` changed `{ __v: 1, ... }` to `{ __v: SCHEMA_VERSION, ... }`, making the assertion tautological. The hardcoded literal was exactly what should have failed — and forced a migration — when `completedTarget` was added. No replacement guard exists; add a test asserting `migrations` has an entry for every version in `0..SCHEMA_VERSION-1`. [src/lib/storage.test.ts:100]
- [x] [Review][Patch] **`storage.ts`'s schema comment is falsified by the same hunk that edits it** — "there is nothing in the registry yet because v1 is the only version that has ever shipped", immediately above `SCHEMA_VERSION = 2` with `migrations[0]` and `migrations[1]` both populated. Same defect class this review round patched five times elsewhere. [src/lib/storage.ts:62-63]
- [x] [Review][Patch] **`reconcileCompletion`'s `overlearningLevel` is optional, where omitting it silently means the minimum level.** Unlike `logCorrect`, where the optional parameter exists for pre-v1.1 backward compatibility, this function is new with exactly one caller — a future caller that forgets the argument gets the lowest possible target and can irreversibly complete a session nowhere near done, with no type error. Make it required. [src/lib/session-transitions.ts:89]
- [x] [Review][Patch] **`Math.max(targetStreak, session.currentStreak)` is unconditional.** The guard on the line above returns for every case where `targetStreak > currentStreak`, so the `max` provably always evaluates to `session.currentStreak`. The seven-line comment, `prd.md`'s `completed_target = max(target_streak, current_streak)`, and the epics AC's "(not the newly-lowered target, **if** the streak exceeds it)" all describe a conditional that cannot be false. Not a behavioural bug — but no test can distinguish this from `completedTarget: session.currentStreak`, and a future reader simplifying in the other direction would change behaviour silently. [src/lib/session-transitions.ts:92-96]
- [x] [Review][Patch] **No error-condition test for the new `writeSession` try/catch** — `grep -n "spyOn\|mockImplementation\|toThrow" src/hooks/useActiveSession.test.ts` returns nothing, so the catch branch could be deleted with the suite green. CLAUDE.md §11.1 requires error-condition tests for new logic. [src/hooks/useActiveSession.test.ts]
- [x] [Review][Patch] **No test that a v0 (un-enveloped) session chains `0 → 1 → 2` to gain `completedTarget`.** `migrate()` does chain correctly (verified), but the existing v0 test asserts identity passthrough only, and the new migration tests seed `__v: 1` exclusively — so the oldest sessions on disk, the ones most in need of the migration, are unexercised. [src/lib/storage.test.ts:132]
- [x] [Review][Patch] **`migrations[1]`'s non-session escape hatch is tested only against an array**, which short-circuits on `!Array.isArray(data)` long before reaching the `'sessionComplete' in data` heuristic. The clause actually carrying risk — a non-array object reaching that heuristic — is never exercised; the test passes with the `in` check deleted entirely. [src/lib/storage.test.ts:124-131]
- [x] [Review][Patch] **`epics.md`'s requirements inventory retains the pre-amendment text of FR22, FR24 and FR37** (`:137`, `:141`, `:250`) — verbatim copies of the three `prd.md` lines this commit amended. [epics.md:137, 141, 250]
- [x] [Review][Patch] **`prd.md:330`'s target-recalculation trigger sentence still lists only `total_incorrect_this_session`** — "Evaluated at session start and re-evaluated after every increment of `total_incorrect_this_session`" — while the transition three lines below states an `OVERLEARNING_LEVEL` change alone re-evaluates `target_streak`. [prd.md:330]
- [x] [Review][Patch] **Record inaccuracies in the commit message and this file.** The commit claims "Full suite (364/364), lint, and typecheck verified after all fixes" while the same changeset documents a non-zero lint exit (1 error + 2 warnings); this file still states "352 tests across 25 suites" in Completion Notes; the Change Log claims "Applied all 19 resulting patches" while the commingled-changeset item remains `[ ]` pending; and the File List still describes an 18-file changeset and says the split "**must happen before either lands**", though the split was performed — `c37b2a8` is this commit's parent and carries every Story 5.1 file. CLAUDE.md §13.3 bars claiming verification that was not run. [this file:75, 76, 158, 173-176]

#### Deferred

- [x] [Review][Defer] A downgrade to any build with `SCHEMA_VERSION = 1` quarantines **every** persisted key — segments, history and settings, not just sessions — because `setObject` now stamps `__v: 2` on all of them and `migrate()` treats an unknown-newer version as unreadable. Quarantine's first-write-wins rule also means a later corruption on the downgraded build cannot be preserved alongside the rollback backup. [src/lib/storage.ts:64, 103-111] — deferred, pre-existing: the unknown-version-is-unreadable rule is documented intent from the 2026-09-05 envelope design (`storage.ts:72-76`); this commit is merely the first bump to make it reachable, and forward-compatible reads are a design change requiring their own decision.

#### Dismissed (1)

The migration referencing `SessionState.completedTarget` while no `types.ts` hunk appears in this commit — the field landed in the parent commit `c37b2a8`, so no build was ever cut between the two and the quarantine-on-upgrade risk the finding posits is unreachable. The record-accuracy half of the same finding is kept as a patch above.

### Review Findings — Round 3 (2026-09-12)

Third review, run against the uncommitted working tree (round 2's patches applied, not yet committed) together with Story 5.4. Three layers planned; **Edge Case Hunter and Acceptance Auditor both terminated on an account billing error before producing output**, so only Blind Hunter (diff-only) ran independently — the remaining analysis was performed in-session by the orchestrator. Coverage is thinner than round 2's; 4 of Blind Hunter's 15 findings were dismissed after verification against the real code.

#### Decisions required

**Both resolved 2026-09-12 by Gerardo.**

**Decision 1 → option 1: count the miss, then reconcile against the new total.** `logIncorrect` increments `totalIncorrectThisSession` first and runs `reconcileCompletion` on the incremented state. A miss is therefore always recorded, and the completion check evaluates the target the session actually has after that miss — so an Incorrect tap that raises `target_streak` back above `current_streak` correctly does *not* complete the session, and falls through to the ordinary zero-the-streak path. This narrows round 2's Decision 2 rather than reversing it: the rescue still fires whenever the streak genuinely meets the post-miss target, but it can no longer complete a session by ignoring the tap that was supposed to count against it. FR10's "recalculates the target whenever a repetition is logged as incorrect" is honoured before completion is evaluated, and the transition's documented field order (streak, then count) is restored in substance.

**Decision 2 → option 1: accept the stale `__v: 2` sessions.** No release has shipped, so the only devices carrying a `{__v: 2, completedTarget: null}` session are development installs; clearing app data costs nothing there, while a `migrations[2]` would be carried by every future build forever to repair a state no user can be in. Not logged to `deferred-work.md`: the window closes permanently at the first release build, since from then on the buggy migration is no longer in any build a user could have run.

- [x] [Review][Decision] ~~**Round 2's Decision 2 dropped the miss it was recording.** `logIncorrect` now reconciles first and, when the reconciliation completes the session, returns immediately — `totalIncorrectThisSession += 1` is never applied on that path (`src/lib/session-transitions.ts:66-74`). The user tapped Incorrect; the tap ends the session but the mistake is not counted. That count is not cosmetic: it is written to the history entry and read back as FR13's "total mistakes" and "total attempts" and FR28's per-entry display, so a completed session permanently understates the run by one miss. The function's own header comment still states the contract three lines above the code as "applied in this exact order ... `total_incorrect_this_session += 1`", citing FR10/FR17 — Decision 2's rationale justifies not *zeroing the streak*, and never addresses not *counting the failure*. The new test asserts `sessionComplete`, `completedTarget` and `currentStreak` on this branch but conspicuously not `totalIncorrectThisSession`, which is the field the change drops; its sibling test on the ordinary branch does assert it. The choice is real because counting the miss first would raise `target_streak`, which can push the target back above `currentStreak` and un-complete the very session the reconciliation was meant to rescue. Options: (1) increment, then reconcile against the new total — a miss can then genuinely prevent the completion it would have had, which is arguably the honest mechanic; (2) reconcile first but still carry the increment onto the completed state — the run is recorded accurately and the completion stands, but `target_streak` at completion no longer matches the `total_incorrect_this_session` recorded alongside it; (3) keep as-is and correct the comment plus the PRD's Incorrect transition to state that a completing Incorrect tap is not counted.~~ — resolved above as option 1. [src/lib/session-transitions.ts:66-74, src/lib/session-transitions.test.ts]

- [x] [Review][Decision] ~~**The migration fix cannot reach the data the buggy migration already rewrote.** Round 2 fixed `migrations[1]` to backfill `completedTarget` from `currentStreak` for an already-complete session — but `SCHEMA_VERSION` is still `2`, and `migrate()` only runs entries for `version < SCHEMA_VERSION` (`src/lib/storage.ts:130-138`). Any session the *previous* `migrations[1]` already processed is on disk as `{ __v: 2, data: { ..., completedTarget: null } }`: it never enters `migrations[1]` again, and the `!('completedTarget' in data)` guard would reject it if it did. The buggy migration is in committed history (it is what `HEAD` contains — `storage.ts` is modified in the working tree), so any device that ran a build from `93b0331`..`25b3844` has already been stamped. The in-code comment says the defect is "Fixed"; it is fixed only for devices that never ran the intermediate build. Every new test seeds `__v: 1` or un-enveloped data — none covers the `__v: 2`-with-null shape, so the gap is invisible to the suite. Reachability is limited (no release has shipped; realistically only Gerardo's own dev device), which is what makes it a decision rather than a patch. Options: (1) `SCHEMA_VERSION = 3` plus a `migrations[2]` backfilling `completedTarget` where it is `null` and `sessionComplete` is `true`, with a test for the `__v: 2` shape; (2) accept — no release exists, and clearing app data on the dev device is cheaper than a permanent migration step; (3) accept but log to `deferred-work.md` as a hard blocker on the first release build.~~ — resolved above as option 1 (accept, no code change, not logged). [src/lib/storage.ts:93-128, 130-138]

#### Patches

**Arising from the resolved decisions:**

- [x] [Review][Patch] `logIncorrect` increments `totalIncorrectThisSession` before reconciling, and reconciles against the incremented state: build the advanced state first, `reconcileCompletion(advanced, level)`, return it if complete, otherwise return `{ ...advanced, currentStreak: 0 }`. Correct the transition's comment accordingly. [src/lib/session-transitions.ts:66-74]
- [x] [Review][Patch] Update the round-2 Incorrect-path test: the completing case must now assert `totalIncorrectThisSession` was incremented, and a new case must pin that a miss which raises the target back above the streak does *not* complete the session and zeroes the streak as normal. The hook-level `logIncorrect` completion-feedback test needs the same increment assertion. [src/lib/session-transitions.test.ts, src/hooks/useActiveSession.test.ts]
- [x] [Review][Patch] Reconcile the PRD's Incorrect transition with the resolved order — the miss is counted first, and completion is then evaluated against the post-miss target, so an Incorrect tap can complete a session only when the streak still meets the raised target. [prd.md:348-350]

**Independent of the decisions:**

- [x] [Review][Patch] **`prd.md:350` still specifies the `Math.max` round 2 deliberately removed.** The Settings-driven completion transition reads "capturing `completed_target = max(target_streak, current_streak)`" while `reconcileCompletion` now assigns `session.currentStreak` directly. Round 2's own patch item argued the `max` "reads as if targetStreak could still win" and changed the code for that reason; the authoritative spec formula was not changed with it (CLAUDE.md §13.4). [prd.md:350]
- [x] [Review][Patch] **`prd.md:347`'s Correct transition is falsified by Decision 1's delegation.** It reads "If `current_streak >= target_streak`, the session completes, capturing `completed_target = target_streak` at that instant." Since `logCorrect` now delegates to `reconcileCompletion`, it captures `currentStreak`. These coincide for an ordinary tap-driven completion, but diverge on exactly the drift case round 2's Decision 1 was resolved to fix: after a settings decrease leaves `currentStreak` above the recalculated target, a Correct tap records the achieved streak, not `target_streak`. The Correct transition's text still describes the pre-decision behaviour. [prd.md:347]
- [x] [Review][Patch] **`epics.md:888` keeps conditional phrasing for an unconditional assignment.** "`completed_target` captures the achieved streak (not the newly-lowered target, **if** the streak exceeds it)" — the `if` describes a branch that cannot be false, since `reconcileCompletion`'s own guard returns early whenever `currentStreak < targetStreak`. Same defect class as the `Math.max` round 2 removed from the code. [epics.md:888]
- [x] [Review][Patch] **Two comments in `session-transitions.ts` are falsified by the code they sit above.** (a) `logCorrect`'s surviving Story 5.1 note says `overlearningLevel` is "threaded straight through to `calculateTargetStreak` — same optional-param posture as that function itself"; it is now resolved to `overlearningLevel ?? OVERLEARNING_LEVEL` before `reconcileCompletion` is called, so `calculateTargetStreak`'s own default is dead on this path. (b) `reconcileCompletion`'s doc block justifies the required parameter as making "a caller forgetting the argument fail to compile rather than silently completing against the minimum level" — but both in-module callers, which are the functions application code actually calls, re-supply exactly that silent default. The stated guarantee holds only for direct `reconcileCompletion` callers, of which there is one. (Verified separately: `OVERLEARNING_LEVEL` is `0.5`, identical to `calculateTargetStreak`'s own default — there is no value drift between the two defaults, only a comment-accuracy problem.) [src/lib/session-transitions.ts:33-36, 94-101]
- [x] [Review][Patch] **The `logCorrect`/`reconcileCompletion` "agreement" test is a tautology.** It builds `advanced` by hand with the same `currentStreak + 1` / `totalCorrectThisSession + 1` expression `logCorrect` uses, then asserts `logCorrect(session, 0.5)` equals `reconcileCompletion(advanced, 0.5)` — true for any implementation of `reconcileCompletion`, correct or broken. It pins that the delegation line exists, not that the rule is right, which is not what the describe's comment claims ("pins that the two paths now agree for every input that reaches both"). The second test in the same describe, titled "agree on completedTarget in the overshoot case", never calls `logCorrect` at all. [src/lib/session-transitions.test.ts:163-188]
- [x] [Review][Patch] **`mockRestore()` is unreachable when the assertion it follows fails.** The reconciliation write-failure test restores the throwing `writeSession` spy on the last line, after two `expect` calls; if either throws, the spy leaks into every subsequent test sharing the `sessionStore` module namespace, producing a cascade of "disk full" failures that mask the real one. Move to `afterEach` or `try/finally`. The same describe also mutates global settings (`setOverlearningPercent(300)` then `(50)`) without restoring them. [src/hooks/useActiveSession.test.ts]
- [x] [Review][Patch] **Dead computation and an undeclared coupling in the hook's `logIncorrect`.** `previousTarget` is computed unconditionally but read only in the `else` branch — its counterpart `nextTarget` was moved inside the branch and this one was left outside. Separately, the completion message interpolates `next.currentStreak` rather than `next.completedTarget`; they are equal only because of `reconcileCompletion`'s current body, so the announcement is coupled to an implementation detail one module away. Round 2 flagged the same two-sources-for-one-number pattern in the screen-reader announcement. [src/hooks/useActiveSession.ts:168-182]

#### Deferred

- [x] [Review][Defer] **The suite is timing-flaky at Jest's default 5 s timeout.** `src/components/SortControl.test.tsx` and `src/components/SegmentListItem.test.tsx` fail with "Exceeded timeout of 5000 ms" on this machine — 6 tests failing under concurrent load, 2 with the machine idle, 0 when re-run with `--testTimeout=30000`. Both suites are untouched by this changeset; individual suites take 28–41 s to initialize under `jest-expo`. CLAUDE.md §11.3 bars flaky tests, and every "N/N passing" claim in these story files is machine- and load-dependent until this is fixed (a `testTimeout` in `jest.config.js` is the obvious lever). — deferred, pre-existing, not caused by this change.
- [x] [Review][Defer] **The `migrations` registry-coverage test only proves definedness.** `expect(migrations[version]).toBeDefined()` passes for `migrations[1] = (d) => d`, so a migration silently reduced to a no-op keeps the tripwire green — and the export widens module-private state to public mutable API to achieve it. A round-trip assertion (seed a `__v: n` payload for each `n`, assert it is not quarantined) would catch a no-op migration without the export. — deferred: the export is deliberate and documented, and the test is still a strict improvement over the tautological assertion it replaced.

#### Dismissed (4)

- **Unchecked `record.currentStreak as number` cast in `migrations[1]` causing data loss.** Verified non-divergent: `isSessionState` requires `isNonNegativeInteger(value.currentStreak)` (`src/lib/types.ts:134`), so a payload with a missing or non-numeric `currentStreak` is quarantined either way — the old `completedTarget: null` path would not have salvaged it. Same for `record.sessionComplete` truthiness vs `=== true`: the guard requires `typeof value.sessionComplete === 'boolean'`.
- **`OVERLEARNING_LEVEL` may be percent-based, making every default-level check off by 100×, and introduces a second default that can drift.** Verified false: `src/lib/mechanic.ts:5,23` — `OVERLEARNING_LEVEL = 0.5` *is* `calculateTargetStreak`'s default parameter value. One constant, no possible drift.
- **A surviving one-argument `reconcileCompletion` call site is now a type error.** Verified false: the sole production call site (`src/hooks/useActiveSession.ts:96`) passes the level, all test call sites pass it, and `npx tsc --noEmit` exits 0.
- **The mount-time auto-completion test cements an FR24 violation as expected behaviour.** The bypass is round 1's explicitly resolved Decision (consequence 5), accepted knowingly and since written into `prd.md` FR24, NFR4, `epics.md` Story 2.10's AC, UX-DR5 and the UX spec's flow diagram. Pinning accepted behaviour with a test is correct, and is what round 2's own patch item asked for.

#### Post-patch verification (2026-09-12)

All round-3 decision-driven and independent patches above applied. Re-verified:
- `npx tsc --noEmit` — clean, exit 0.
- `npx jest --testTimeout=30000` — 413/413 tests passing, 25/25 suites (see the deferred flaky-timeout entry above for why `--testTimeout` is needed for a reproducible run).
- `npx eslint .` — 1 error, 2 warnings, all pre-existing, none introduced by this round's changes.

## Dev Notes

### Architecture compliance

- **No new mechanism for the core AC.** `target_streak` (`useActiveSession`'s returned `targetStreak`) is derived fresh on every render from `calculateTargetStreak(session?.totalIncorrectThisSession ?? 0, overlearningLevel)`, and `overlearningLevel` is itself derived fresh from `useSettings().settings.overlearningPercent` — both re-run on every render, and `useSettings()`'s `useSyncExternalStore` subscription triggers a re-render on any `settings.general` write. This is the payoff of the v1.0 derive-on-read decision the architecture doc credits (architecture.md's "FR37 requires no additional mechanism" note) — Task 1 exists to *prove* this with a real end-to-end test, since nothing in the codebase currently exercises "change the setting after `start()`, before completion."
- **Deferred Finding (Story 5.1 code review, 2026-09-10) — this is what Task 3 verifies/fixes:** `sessionComplete` is not derived on every read the way `targetStreak` is — it is a stored boolean, written only inside `session-transitions.ts`'s `logCorrect` transition. Lowering the overlearning-% mid-session (reachable today: Home's gear icon is unconditionally present, and backing out of an in-progress session to Home does not discard it) can leave `currentStreak >= targetStreak` (live) true while `sessionComplete` stays `false`, until the user's next Correct/Incorrect tap re-evaluates it. This was explicitly deferred to this story and Story 5.3 in `deferred-work.md` — Task 3 is that follow-through. **This is a proposed design, not yet confirmed correct against the real code** — the dev agent must read `useActiveSession.ts`'s own header comment (rapid-tap and multi-instance guarantees) before committing to the `useEffect` mechanism outlined in Task 3, and halt to ask rather than pick a different approach unilaterally if it looks unsafe.
- **`session.completedTarget` (new in Story 5.1's review fix, `src/lib/types.ts`).** Captures the target actually met, in the same write that flips `sessionComplete` true (`session-transitions.ts`'s `logCorrect`), specifically so a settings change *after* completion but *before* Done can't alter what gets permanently recorded (`complete()` in `useActiveSession.ts` already prefers it). Task 2 closes the matching *display*-side gap: the Completion screen (`CompletionScreen.tsx`, rendered from `session/[id].tsx`) was still reading the live `targetStreak`, not this anchored value, so what the user sees and what gets recorded could show different numbers for the same completed session.
- **No change to `calculateTargetStreak`, `logCorrect`'s signature, or the settings storage boundary.** This story is verification plus two narrow, adjacent-gap fixes — not a re-implementation of Story 5.1's threading. Do not touch `lib/mechanic.ts` or `lib/settings.ts`.

### Existing code confirmed by direct inspection

- **`src/hooks/useActiveSession.ts`** (read in full): `overlearningLevel` is computed once per render from `useSettings().settings.overlearningPercent / 100`; `targetStreak` (the hook's returned value) is computed fresh every render from it. No caching, no `useMemo` with a stale dependency array — confirmed nothing here would prevent AC #1 from already working for the *display number* alone.
- **`src/app/session/[id].tsx`** (read in full): line ~151, `<CompletionScreen session={session} segmentName={segment.name} finalTarget={targetStreak} onDone={handleDone} onRepeat={handleRepeat} />` — confirmed this is the live value, not `session.completedTarget`, which is what Task 2 fixes. Line 146: `if (session.sessionComplete) { return <CompletionScreen ... /> }` — confirms the screen decides which UI to show purely from the stored `sessionComplete` flag, which is the reason Task 3's reconciliation (if it reproduces) must correct the *stored* flag, not just a hook-local derived value — a derived-only fix would desync this conditional from the lockout guards in `logCorrect()`/`logIncorrect()`.
- **`src/components/session/CompletionScreen.tsx`** (read in full): `finalTarget: number` is a plain prop, displayed verbatim in `completion-stats`'s "Target reached: {finalTarget}" line. No internal computation — confirms Task 2's fix is a one-line caller-side change, no component API change needed.
- **`src/components/session/StreakReadout.tsx`** (read in full): `targetStreak` is a plain prop, rendered at `streak-readout`'s `props.children[2]`, used by Task 1's assertion.
- **`src/app-tests/session.test.tsx`** (read in full): existing tests use `useLocalSearchParams.mockReturnValue({ id: segment.id })` then `render(<ActiveSessionScreen />)`; streak values are asserted via `view.getByTestId('streak-readout').props.children[0]` (current) and `[2]` (target). Task 1's new test follows this exact pattern.
- **`src/hooks/useActiveSession.ts`**'s header comment (read in full) documents two invariants any Task 3 fix must not break: (1) every mutator reads `sessionStore.readSession()` fresh rather than closing over a render's `session`, to compound rapid taps instead of dropping one; (2) two mounted instances (Home + Active Session screen) must stay in sync via the shared MMKV subscription, not local state.

### Previous story intelligence (5.1)

- Story 5.1's code review (3-layer adversarial: Blind Hunter, Edge Case Hunter, Acceptance Auditor) is the direct origin of Tasks 2 and 3 — both were explicitly deferred to "Story 5.2's verification pass" in `deferred-work.md`, not discovered independently here. Read that entry (`_bmad-output/implementation-artifacts/deferred-work.md`, heading "Deferred from: code review of 5-1-configure-the-overlearning-target (2026-09-10)") before starting Task 3 — it has the full reachability trace for how a user gets to Settings mid-session.
- Story 5.1 also fixed (in the same review pass) a stepper double-tap race in `settings.tsx` by reading `readSettings()` fresh inside `onPress` rather than closing over a render-time value — worth knowing as precedent if Task 3's fix needs a similar "read fresh, don't trust the render closure" pattern.
- Story 5.1's `useActiveSession.test.ts` additions already demonstrate the `setOverlearningPercent()`-then-assert-on-`targetStreak` pattern Task 1 extends — search that file for `setOverlearningPercent` to find them; the difference this story's tests must capture is calling it *after* `start()` and some taps, not before.

### Git intelligence

Last commit at story-creation time: `27a168a` "Add Configure the Overlearning Target settings screen (FR35, FR36)". No dependency changes; this story needs none either.

**[Review][Patch] found via code review 2026-09-11 — corrected:** the sentence above previously claimed `27a168a` "includes the post-review fixes described above (`SessionState.completedTarget`, the stepper race fix, doc corrections)". Verified false: `git grep -n "completedTarget" 27a168a -- src` returns nothing, and those fixes were still uncommitted working-tree changes at review time, commingled with this story's own Task 2/3 work (see File List below and CLAUDE.md §13.2 on commit granularity — the changeset needs splitting into two commits, Story 5.1's post-review fixes and Story 5.2's own work, before either lands).

### Project Structure Notes

**Modified:**
- `src/app/session/[id].tsx` — Task 2's one-line `finalTarget` prop change
- `src/hooks/useActiveSession.ts` — Task 3's reconciliation effect, only if the edge case reproduces
- `src/app-tests/session.test.tsx` — new tests for Tasks 1 and 2
- `src/hooks/useActiveSession.test.ts` — new test for Task 1's hook-level assertion, new test(s) for Task 3
- `_bmad-output/implementation-artifacts/deferred-work.md` — mark the Story 5.1 entry resolved once Task 3 is done

**Not touched:**
- `src/lib/mechanic.ts`, `src/lib/settings.ts`, `src/lib/session-transitions.ts` — no formula or storage-boundary changes in this story
- `src/app/settings.tsx` — no UI changes; FR39's standing notice is Story 5.3's scope, not this one's

### References

- [Source: _bmad-output/planning-artifacts/epics.md#Story-5.2-Apply-a-Changed-Target-to-an-In-Progress-Session] — story statement, AC, and the "no new mechanism" framing this story's Tasks 2–3 add nuance to
- [Source: _bmad-output/planning-artifacts/prd.md] (line 269) — "`OVERLEARNING_LEVEL`... A change applies immediately to `target_streak`'s next recalculation, including for a session already in progress — from v1.1 the formula below always reads the current value, never a value captured at session start." Confirms Task 2's `completedTarget` anchoring is about *recording/displaying what was achieved*, not about changing the live formula's own always-current-value behavior.
- [Source: _bmad-output/planning-artifacts/prd.md] (line 354) — FR37 full text
- [Source: _bmad-output/planning-artifacts/ux-design-specification.md] (line 539) — "Mid-session change... No UI is specified for this on the Settings screen... Deliberately not designed: a warning or confirmation when changing the setting while a session is active." Confirms this story adds no new UI; FR39's warning is Story 5.3's job.
- [Source: _bmad-output/planning-artifacts/architecture.md] (~line 552) — "FR37... requires no additional mechanism" — the claim Task 1 verifies with a real test rather than taking on faith.
- [Source: _bmad-output/implementation-artifacts/5-1-configure-the-overlearning-target.md] — Dev Agent Record's post-review Completion Notes, and the Review Findings section, for the full context behind Tasks 2 and 3
- [Source: _bmad-output/implementation-artifacts/deferred-work.md] — the exact deferred entry Task 3 resolves

## Dev Agent Record

### Agent Model Used

Claude Sonnet 5 (claude-sonnet-5), via bmad-dev-story.

### Debug Log References

None — no failing runs beyond the deliberate red-phase tests (Task 2's and Task 3's reproduction tests, both confirmed genuinely red before their fixes, per TDD).

### Completion Notes List

- Task 1: AC #1 was already satisfied by Story 5.1's plumbing — both new tests (screen-level and hook-level) passed on first run, no code change needed. This confirms the epics.md "no new mechanism" framing for the *target number* specifically.
- Task 2: found and fixed a real display/history divergence — `session/[id].tsx`'s `CompletionScreen` was reading the live `targetStreak` instead of the historically-anchored `session.completedTarget` (added in Story 5.1's own code review), so a settings change made while sitting on the Completion screen (before Done) could show one number while a different one got permanently recorded. One-line fix, confirmed red before and green after.
- Task 3: the deferred edge case from Story 5.1's review reproduced exactly as described — `sessionComplete` stayed `false` after a settings change alone brought the live target down to an already-achieved streak. Fixed with a `useEffect` in `useActiveSession()`, keyed on `overlearningLevel`, that reads the session fresh (not the render closure) and reconciles `sessionComplete`/`completedTarget` the same way `logCorrect`'s transition would. Verified safe against both invariants documented in `useActiveSession.ts`'s header comment (rapid-tap compounding, two-mounted-instances sync) before implementing, per the story's own HALT-if-unsafe instruction — did not need to HALT. `deferred-work.md` updated to mark the entry resolved.
- Full test suite (352 tests across 25 suites, up from 347 pre-story) and typecheck pass clean. **[Review][Patch] found via code review 2026-09-11 — corrected:** this line previously also claimed "lint ... all pass clean." Verified false: `npx eslint .` exits with 1 error (`use-color-scheme.web.ts:11`, `react-hooks/set-state-in-effect`) and 2 warnings (the pre-existing `session/[id].tsx:106` `exhaustive-deps` warning correctly identified above, plus an unrelated `.expo/types/router.d.ts` unused-directive warning). All three are pre-existing and untouched by this story's diff — confirmed via `git stash`, matching the original note's own verification method — but "clean" overstated a non-zero exit code.
- **Code review 2026-09-11 (3 adversarial layers) round:** applied all 12 defect patches and all 7 spec-synchronization patches arising from the 3 resolved decisions (see Review Findings above) — `reconcileCompletion` extracted to `lib/session-transitions.ts` as the shared completion-writing transition, `completedTarget` now captures `Math.max(liveTarget, currentStreak)`, the reconciliation write is try/caught and fires `feedback.completion(...)` for parity with tap-driven completion, a `SCHEMA_VERSION` v1→v2 migration was added for the previously-unmigrated `completedTarget` field, and prd.md/epics.md/architecture.md were corrected to document the settings-driven completion transition this story's Task 3 introduced. The `feedback.completion(...)` call surfaced a fresh `react-hooks/exhaustive-deps` lint error (`useFeedbackSignal()` returns a new object every render, and adding it to the effect's dependency array would defeat the deliberate `overlearningLevel`-only trigger) — resolved with a `feedbackRef` updated in its own effect, matching the file's existing fresh-read posture; `npx eslint .` confirmed back to the pre-existing baseline (1 error + 2 warnings, none touched by this diff) before closing this round. Full suite re-verified after fixes (see Change Log).
- **Code review 2026-09-12 (round 2, 3 adversarial layers under Opus 5) round:** applied all 21 resulting patches (2 arising from the 2 resolved decisions plus 2 shared/agreement tests, plus 17 independent findings). `reconcileCompletion` is now `logCorrect`'s and `logIncorrect`'s single shared completion rule (one rule, not two); `logIncorrect` retries a missed reconciliation before zeroing the streak so a miss cannot destroy an already-achieved run; `reconcileCompletion`'s `overlearningLevel` parameter is now required, not optional; `completedTarget` now records `session.currentStreak` directly (the `Math.max` was provably unconditional, per the finding); the v1→v2 migration now backfills `completedTarget` from `currentStreak` for an already-complete pre-Story-5.2 session, not just an in-progress one; added tests for the mount-time/app-open auto-completion consequence and for the reconciliation write's error path (CLAUDE.md §11.1); added `migrations` registry-coverage, v0→v1→v2 chain, and non-array-object escape-hatch tests to `storage.test.ts`; corrected stale spec text in `prd.md`, `epics.md`, `architecture.md`, and `ux-design-specification.md` (FR20–FR22/FR24/FR37/FR39 annotations, NFR4/UX-DR5/Story 2.10's AC reconciled with the accepted app-open bypass, the `completed_target` schema addition documented in both authoritative field lists, the "reversible" UX-spec claims corrected, FR13/FR28/UX-DR4 reworded to "the streak achieved" rather than "the final target achieved", and this file's own AC section extended with AC #2). A pre-existing (not introduced by this round) `tsc --noEmit` error in `storage.ts`'s `migrations[1]` double-cast was also found and fixed while touching that function. Full suite (401/401 across 25 suites), lint (pre-existing baseline: 1 error + 2 warnings, untouched by this diff), and typecheck (clean) re-verified after fixes.
- **Code review 2026-09-12 (round 3; Blind Hunter only — Edge Case Hunter and Acceptance Auditor both failed on an account billing error, analysis performed in-session) round:** 2 decisions resolved by Gerardo (count the miss before reconciling, so a completing Incorrect tap no longer drops the mistake it was recording; accept the round-2 migration's stale-`__v: 2` gap rather than adding a third migration, since no release has shipped). Applied all 3 decision-driven patches (the `logIncorrect` reorder, its test updates including a new counted-miss-prevents-completion boundary case, and the matching PRD Incorrect-transition correction) plus 6 independent patches: reconciled `prd.md`'s Correct-transition and Settings-driven-completion formulas (both still described the pre-round-2 `target_streak`/`Math.max` behavior) and `epics.md`'s AC #2 conditional phrasing with the code's unconditional rule; corrected two comments in `session-transitions.ts` that overclaimed straight-through parameter threading and a compile-time guarantee the module's own callers don't provide; replaced a tautological `logCorrect`/`reconcileCompletion` "agreement" test with a black-box comparison that can actually fail; fixed a spy-leak risk in the reconciliation-write-failure test (`afterEach` instead of an end-of-test `mockRestore()`); and fixed a dead computation plus an implementation-detail coupling in the hook's `logIncorrect` (now reads `next.completedTarget`, not `next.currentStreak`, in its completion message). Full suite (413/413 across 25 suites), lint (pre-existing baseline, unchanged), and typecheck (clean) re-verified after fixes.

### File List

**[Review][Patch] found via code review 2026-09-11 — corrected:** this section previously listed only the 5 files Story 5.2's own tasks touched. The actual uncommitted changeset at review time contained 18 files, because Story 5.1's post-review fixes were never committed (see "Git intelligence" correction above) and are commingled here. Both groups are listed separately below. **The split into two commits is a commit-time git operation, not a code edit** — it happens when this story is committed, per Gerardo's separate authorization for that action (see the still-pending patch item under Round 1's "Patch — defects, independent of the decisions"); it has not been performed yet, and this section does not claim otherwise.

**Story 5.2's own changes:**
- `src/app/session/[id].tsx` — Task 2: `CompletionScreen`'s `finalTarget` prop now prefers `session.completedTarget`; round 1 review: corrected a stale comment
- `src/hooks/useActiveSession.ts` — Task 3: reconciliation `useEffect`; round 1 review: delegates to `lib/session-transitions.ts`'s new `reconcileCompletion`, wraps the write in try/catch, fires `feedback.completion(...)`, corrected two stale comments; round 2 review: corrected the `catch` comment's overstated recovery claim; round 3 review: `logIncorrect` no longer computes `previousTarget` unconditionally, and its completion message reads `next.completedTarget` instead of `next.currentStreak`
- `src/lib/session-transitions.ts` — round 1 review: new `reconcileCompletion` export (was hand-built inline in the hook); round 2 review: `logCorrect` delegates its completion branch to `reconcileCompletion` (single rule); `logIncorrect` reconciles before zeroing the streak; `reconcileCompletion`'s `overlearningLevel` made required; `completedTarget` now records `currentStreak` directly, not an unconditional `Math.max`; round 3 review: `logIncorrect` now counts the miss (`totalIncorrectThisSession += 1`) before reconciling, not after; corrected two stale comments (parameter-threading claim, "fail to compile" guarantee scope)
- `src/lib/storage.ts` — round 1 review: `SCHEMA_VERSION` v1→v2, `migrations[1]` to default a missing `completedTarget`; round 2 review: migration also backfills `completedTarget` from `currentStreak` for an already-complete pre-Story-5.2 session; corrected the stale schema comment; fixed a pre-existing `tsc` double-cast error in `migrations[1]`
- `src/app-tests/session.test.tsx` — new tests for Tasks 1 and 2; round 1 review: new screen-level Task 3 test
- `src/hooks/useActiveSession.test.ts` — new tests for Task 1 (hook-level) and Task 3; round 1 review: fixed a vacuous negative test, added boundary/overshoot/feedback-parity coverage; round 2 review: added mount-time auto-completion and reconciliation-write-failure tests; round 3 review: the reconciliation-write-failure test's spy cleanup moved to `afterEach`, replacing an end-of-test `mockRestore()` that a failed assertion would have skipped
- `src/lib/session-transitions.test.ts` — round 1 review: new `reconcileCompletion` test suite; round 2 review: added the `logCorrect`/`reconcileCompletion` agreement test and the Incorrect-tap-completes-instead-of-zeroing test; round 3 review: replaced the tautological agreement test with a black-box comparison; added a new `totalIncorrectThisSession`-on-completion assertion and a counted-miss-prevents-completion boundary case
- `src/lib/storage.test.ts` — round 1 review: fixed a hardcoded `__v: 1` assertion, added migration tests; round 2 review: added migrations registry-coverage, v0→v1→v2 chain, already-complete-session backfill, and non-array-object escape-hatch tests
- `_bmad-output/implementation-artifacts/deferred-work.md` — Story 5.1's deferred entry marked resolved; round 1 review: 2 new deferred entries appended; round 3 review: 2 more deferred entries appended (test-timeout flakiness, migrations registry-coverage test weakness)
- `_bmad-output/planning-artifacts/prd.md`, `epics.md`, `architecture.md` — round 1 review: spec-synchronization patches (see Review Findings above); round 2 review: further spec-synchronization patches (FR20–FR22/FR24/FR37/FR39 annotations, NFR4/UX-DR5/Story 2.10's AC, the `completed_target` schema addition, FR13/FR28/UX-DR4 completion-summary wording, the target-recalculation trigger sentence, and the remaining stale "no new mechanism" / "not a separate mechanism" claims); round 3 review: corrected the Correct/Incorrect/Settings-driven-completion transition text in `prd.md` (delegation to a shared rule; miss counted before reconciling; `current_streak`, not `max()`) and `epics.md`'s AC #2 conditional phrasing
- `_bmad-output/planning-artifacts/ux-design-specification.md` — round 2 review: corrected the "reversible by stepping back" claims and the resume/discard-prompt mandatoriness statement
- This file — round 1 review: Review Findings section, Dev Agent Record corrections; round 2 review: Round 2 Review Findings section, AC #2, further Dev Agent Record corrections; round 3 review: Round 3 Review Findings section, further Dev Agent Record corrections

**Story 5.1's uncommitted post-review work (pre-existing in the working tree, not authored by this story):**
- `src/lib/types.ts`, `src/lib/types.test.ts` — `SessionState.completedTarget`
- `src/lib/segments.test.ts`, `src/app-tests/home-session-interaction.test.tsx`, `src/app-tests/index.test.tsx`, `src/lib/settings.test.ts`, `src/lib/session-transitions.test.ts` (pre-review-round content), `src/lib/mechanic.ts`, `src/app/settings.tsx`, `src/app-tests/settings.test.tsx` — Story 5.1's code review fixes (stepper race, comment corrections)
- `_bmad-output/implementation-artifacts/5-1-configure-the-overlearning-target.md` — Story 5.1's status flip to `done`, for code not yet committed

### Change Log

- 2026-09-10: Implemented Story 5.2 — verified AC #1 (mid-session target display, already working via Story 5.1's derive-on-read plumbing) with new end-to-end tests; fixed the Completion screen's `finalTarget` to anchor on `session.completedTarget` instead of a possibly-since-changed live value; fixed the deferred `sessionComplete` staleness edge case with a settings-change-triggered reconciliation effect. Full suite 352/352, typecheck clean (lint claim corrected above).
- 2026-09-11: Code review (3 adversarial layers: Blind Hunter, Edge Case Hunter, Acceptance Auditor). 3 decisions resolved by Gerardo (keep the settings-driven completion trigger and specify its consequences; record the achieved streak in `completedTarget`; amend the PRD's monotonicity sentence). Applied all 19 resulting patches: extracted `reconcileCompletion` to the transitions layer, added the missing `SCHEMA_VERSION` migration, guarded the reconciliation write, added completion-feedback parity, corrected 5 stale comments, fixed 2 test defects and added boundary/overshoot/screen-level coverage, corrected `architecture.md`'s FR37 paragraph and file-tree line, corrected this file's own Dev Agent Record, and synchronized `prd.md`/`epics.md` with the newly-specified settings-driven completion behavior. 2 pre-existing findings deferred to `deferred-work.md`. Full suite, lint, and typecheck re-verified after fixes.
- 2026-09-12: Round 2 code review (3 adversarial layers under Opus 5, against commit `93b0331`). 32 raw findings → 25 unique → 1 dismissed. 2 decisions resolved by Gerardo (make `reconcileCompletion` `logCorrect`'s and `logIncorrect`'s single shared completion rule; reconcile on the `logIncorrect` path too, so a miss cannot destroy an already-achieved run). Applied all 21 resulting patches: the 2 decision-driven code changes plus their agreement/completion tests; fixed the v1→v2 migration's re-introduced display/history-divergence bug for already-complete sessions; made `reconcileCompletion`'s `overlearningLevel` required; replaced the unconditional `Math.max` with a direct `currentStreak` assignment; added mount-time-auto-completion and reconciliation-write-failure tests; added `storage.test.ts` registry-coverage, v0→v1→v2 chain, and non-array-escape-hatch tests; synchronized `prd.md`, `epics.md`, `architecture.md`, and `ux-design-specification.md` with the behavior this round's decisions and round 1's already-accepted consequences imply, including this file's own AC section (added AC #2) and Dev Agent Record. Also fixed one pre-existing `tsc --noEmit` error in `storage.ts`'s `migrations[1]`, found while touching that function. Full suite (401/401 across 25 suites), lint (pre-existing baseline unchanged), and typecheck (now clean) re-verified after fixes.
